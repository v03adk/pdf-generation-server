{% extends "@Theme/layout.html.twig" %}

{% block content %}
    <h1>Frontpage</h1>

    <a href="{{ path("templates_index" )}}" class="btn btn-lg btn-primary">View all the templates!</a>
    
{% endblock %}